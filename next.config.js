/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['s1.ax1x.com'],
    formats: ['image/avif', 'image/webp'],
  },
}

module.exports = nextConfig